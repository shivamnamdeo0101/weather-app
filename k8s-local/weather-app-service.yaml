apiVersion: v1
kind: Service
metadata:
  name: weather-app
  namespace: dev
  labels:
    app: weather-app
    environment: dev
spec:
  type: LoadBalancer
  selector:
    app: weather-app
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  