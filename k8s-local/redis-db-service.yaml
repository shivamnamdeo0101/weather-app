apiVersion: v1
kind: Service
metadata:
  name: redis-db
  namespace: dev
  labels:
    app: redis-db
    environment: dev
spec:
  selector:
    app: redis-db
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP  # internal only